{
  "name": "4chan-image-scroller-client",
  "version": "1.0.0",
  "description": "Client for 4chan image scroller",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "start-local": "export NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "build": "NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cors": "^2.8.5",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-infinite-scroll-component": "^6.1.0",
    "react-scripts": "^4.0.3"
  },
  "author": "",
  "license": "ISC",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
